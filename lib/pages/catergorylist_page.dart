import 'package:flutter/material.dart';

class CatergoryListPage extends StatelessWidget {
  const CatergoryListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Categories'),
      ),
    );
  }
}
